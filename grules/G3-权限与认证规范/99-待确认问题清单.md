# 99 · 已确认决策清单（G3）

> 引用：[00-index.md](./00-index.md)
> 状态：**全部 12 项已由 PM 决策完毕，规则文件已同步更新。** 本文件保留作为决策审计记录。

---

| 编号 | 问题 | PM 最终决策 ✅ | 落点（已同步） |
|------|------|---------------|---------------|
| Q1 | 第三方人机验证（Turnstile / hCaptcha）| **不使用第三方服务**，仅靠后端节流（IP 1h ≤ 5 次注册、email+ip 5/15min 锁定、邮件 60s/1h 节流）| 02 §五 / 05 §五；移除 Turnstile 字段 |
| Q2 | 邮箱注册必须先验证邮件 | **是**，`MAILER_AUTOCONFIRM=false`；本期**邮件发送 = Mock**（写入 `.dev/mailbox/`），prod 接真实 SMTP | 02 §八 / 05 §一 |
| Q3 | GoTrue `before_user_created` Hook | **优先用 Hook 做注册前风控**（邮箱/IP/邀请码/默认 role 注入）；GoTrue 版本不足时降级 DB trigger，仅做创建后初始化与标记，不能阻断注册；部署前需校验镜像版本 ≥ v2.130 | 05 §四（含主方案 + 降级方案 + 选型决策点）|
| Q4 | 凭证存储 = httpOnly Cookie 还是 localStorage | **升级到 httpOnly Cookie**（PM：安全第一）：`zhiyu-at` / `zhiyu-rt` 均 `HttpOnly; Secure; SameSite=Lax`；外加 `zhiyu-csrf` Double-Submit Token；supabase-js 自定义 storage adapter 走 `/api/auth/cookie/*` 代理接口 | 00 §核心原则 9 / 02 §二 / 03 §二.4 / 03 §四 新增 `AUTH_CSRF_INVALID` |
| Q5 | 多设备数量上限 | **硬编码 3**，写入 `packages/shared/src/auth/constants.ts` | 00 §核心原则 5 |
| Q6 | 登录失败锁定阈值 | **5 次 / 15 分钟**，按 `email + ip` 维度（保持原方案）| 02 §五 |
| Q7 | "我的设备"页 | **不做该功能**，有问题重新登录即可；`user_sessions` 表仅用于"踢最早 + 禁用全踢"内部用途 | 02 §4.4 / 02 删 §4.5 |
| Q8 | 找回密码后撤销其他设备 | **撤销**：`updateUser({ password })` 成功后调 `auth.admin.signOut(scope=others)` + 删 `user_sessions` 中其他行 | 02 §七 |
| Q9 | 管理端 TOTP / 2FA | **本期不做 TOTP**（PM 决策变更）；超管仅靠强密码 + 登录节流保护；后续安全升级计划 | 02 §1.3（已删除 TOTP 流程）；04 §五（已删 admin_totp_* 表）；03 §四（已删 AUTH_TOTP_* 错误码）|
| Q10 | i18n 错误提示 | **本期完成 5 语言**：zh / en / vi / th / id；所有 `AUTH_*` code、邮件模板、登录/注册/重置/禁用提示统一 5 语言（系统所有操作和提示遵循此规则）| 00 §核心原则 12 |
| Q11 | 禁用账号是否立即踢出全设备 | **立即踢出**：`update profiles + auth.admin.signOut(global) + delete user_sessions + 清缓存 + audit_logs`；**支持重新启用**（`is_disabled=false`，用户下次登录即恢复）| 02 §6.1 |
| Q12 | super_admin 忘密 / 逃生通道 | **不设逃生通道**：账号与初始密码放在项目 `.env` 的 `SUPER_ADMIN_EMAIL` / `SUPER_ADMIN_PASSWORD`，首次部署 seed 创建；丢失时**运维直接在 DB 用相同 seed 流程手工插入新超管**；技术上**允许多超管**，但管理端 UI 不提供创建入口（默认仍只 1 个）| 00 §核心原则 2 / §核心原则 7 / 05 §三（新增 §3.1 .env / §3.4 手工逆生流程）|

---

## 后续可能新增的开放项（非本期阻塞）

> 以下为开发推进中暴露但 PM 已表态可后续补的内容，仅记录避免遗忘。

- 多超管场景下"禁用其他超管"的二次确认 UX（当前仅后端允许）。
- TOTP 恢复码用尽时的重发流程（当前需运维 DB 操作清表后重新绑定）。
- Cookie 跨子域共享细节（`api.zhiyu.com` / `app.zhiyu.com` / `admin.zhiyu.com` 设置 `Domain=.zhiyu.com`，prod 部署时确认）。
- prod SMTP 接入（Q2 mock 替换为真实邮件服务）。

> 后续如有新增不确定项，请按本表格式追加；落地后追加 ✅ 与最终决策。
