# F2-AI：接口规范

> **谁来写**：AI根据《F2-用户：操作与业务逻辑》生成
> **谁来审**：产品经理
> **何时引用**：编码阶段逐个开发接口时使用。每次只取其中一个接口的定义喂给编码AI。
>
> **AI指令**：
> 1. 接口设计严格遵循G1-AI中的API规范（路径命名、响应格式、错误码）
> 2. 数据结构严格遵循F1-AI中的表定义
> 3. 每个接口必须标注"对应需求中的操作"
> 4. PM未提到但你认为需要补充的接口，放在"AI补充的接口"区域

---

## 功能名称：`[从F2-用户复制]`

## 接口总览

| 序号 | 接口名称 | 方法 | 路径 | 对应需求中的操作 |
|------|---------|------|------|----------------|
| 1 | | | | |
| 2 | | | | |

---

## 接口详细定义

### 接口 1：`[接口名称]`

**对应需求中的操作**：`[F2-用户中的第几项操作]`

**方法**：`[GET/POST/PUT/DELETE]`

**路径**：`[遵循G1-AI的URL命名规则]`

**权限**：`[哪些角色，引用F2-用户中的权限定义]`

**业务校验**（在参数校验之外的业务逻辑校验）：
```
[例如：]
- 订单状态必须为"pending_confirm"才能调用此接口
- 当前用户必须有该操作权限
```

**请求参数**：

| 参数名 | 位置 | 类型 | 必填 | 说明 | 校验规则（引用F1-AI） |
|--------|------|------|------|------|---------------------|
| | | | | | |

**成功响应**（标注HTTP状态码）：

```json
{
}
```

**错误场景**：

| HTTP状态码 | 业务错误码 | 触发条件 | 错误信息 |
|-----------|-----------|---------|---------|
| 400 | | 参数校验失败 | |
| 401 | | 未登录 | |
| 403 | | 无权限 | |
| 404 | | 资源不存在 | |
| 409 | | 状态冲突（如订单已被处理） | |

---

### 接口 2：`[下一个接口]`

`[重复上面的完整格式]`

---

`[对每个接口重复]`

---

## AI补充的接口

> 需求中没有明确提到，但技术上需要的接口。

| 接口名称 | 用途 | 补充理由 |
|---------|------|---------|
| | | |

---

## 并发与冲突处理方案

```
[根据F2-用户中的边界情况，定义技术上的并发处理方案]

场景：[例：两人同时处理同一订单]
方案：[乐观锁 / 悲观锁 / 先到先得 + 前端提示]
实现方式：[具体的技术实现]
```

---

## 待确认问题清单

| 编号 | 问题 | AI的默认方案 | 需要PM确认 |
|------|------|------------|-----------|
| Q1 | | | |

---

> **PM审核要点**：
> ✅ 接口数量是否覆盖了你列出的所有操作
> ✅ 每个接口的权限是否正确
> ✅ 错误场景是否覆盖了你提到的边界情况
> ✅ AI补充的接口是否有必要
> ✅ 并发处理方案是否符合你的预期
