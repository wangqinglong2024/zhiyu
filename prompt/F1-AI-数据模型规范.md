# F1-AI：数据模型规范

> **谁来写**：AI根据《F1-用户：数据与业务规则》生成
> **谁来审**：产品经理
> **何时引用**：编码阶段建表和定义类型时使用；F2-AI和F3-AI生成时也需引用本文档。
>
> **AI指令**：
> 1. 所有命名规范遵循G1-AI中的数据库规范
> 2. 每个字段都必须标注"对应需求中的信息"列，让PM能一一核对
> 3. 不确定的地方列在待确认问题清单中
> 4. PM没提到但你认为技术上必须补充的字段，放在"AI补充字段"区域并说明理由

---

## 功能名称：`[从F1-用户复制]`

---

## 一、表定义

### 表：`[表名]`

**对应需求中的对象**：`[F1-用户中的中文对象名]`

| 字段名 | 类型 | 必填 | 默认值 | 索引 | 对应需求中的信息 | 校验规则 |
|--------|------|------|--------|------|----------------|---------|
| | | | | | | |

**AI补充字段**（需求中未提及，但技术上需要）：

| 字段名 | 类型 | 补充理由 |
|--------|------|---------|
| | | |

### 表：`[第二张表，如有]`

`[重复上面的格式]`

---

## 二、枚举定义

### `[枚举名称]`

| 枚举值 | 对应需求中的状态名 | 显示文本 | 标签颜色（遵循G2-AI色彩映射） |
|--------|----------------|---------|--------------------------|
| | | | |

---

## 三、表关系

```
[精确描述表之间的关系，包括外键和删除级联策略]

[表A] 1 ←→ * [表B]  (通过字段 xxx 关联)
  删除策略：[级联删除 / 置空 / 禁止删除]

[其他关系...]
```

---

## 四、状态机（如有）

### 状态转换图

```
[AI用文本图画出状态转换]
```

### 状态转换规则

| 从 | 到 | 对应需求 | 校验条件 | 字段变更 |
|----|---|---------|---------|---------|
| `[枚举值]` | `[枚举值]` | `[引用F1-用户中的哪一行]` | `[必须满足什么条件]` | `[哪些字段会被更新]` |

### 禁止的转换

```
[列出所有不允许的状态变化，后端必须校验]
```

---

## 五、校验规则汇总

> 前后端共用。

| 字段 | 规则 | 正则/范围 | 前端提示语 | 后端错误码 |
|------|------|---------|----------|-----------|
| | | | | |

---

## 六、计算规则（如有）

```
[将PM的自然语言计算规则翻译为精确的公式和实现方案]

字段名 = 公式
存储精度：[以分为单位的整数 / 其他]
显示格式：[¥XX.XX / 其他]
边界情况：[除数为0时 / 金额溢出时...]
```

---

## 七、编号生成规则（如有自动编号）

```
格式：[例：ORD-{YYYYMMDD}-{NNN}]
序号重置：[每天重置 / 全局递增]
并发安全：[如何保证编号不重复]
```

---

## 八、种子数据（推荐）

```json
[AI生成一组测试数据，覆盖各种状态]
```

---

## 待确认问题清单

| 编号 | 问题 | AI的默认方案 | 需要PM确认 |
|------|------|------------|-----------|
| Q1 | | | |

---

> **PM审核要点**：
> ✅ 每个需求中的信息是否都有对应的字段
> ✅ 字段类型和校验规则是否符合你的要求
> ✅ 状态枚举是否完整、转换规则是否正确
> ✅ AI补充的字段是否合理
> ✅ 计算规则是否准确
