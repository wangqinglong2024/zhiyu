# F1-用户：数据与业务规则

> **谁来写**：你（产品经理）
> **写完后做什么**：把本文档 + [G1-AI架构与技术规范] + F1-AI空模板 一起发给AI
> **提示词**：`"请根据我的数据描述，参照G1-AI中的数据库规范，按F1-AI模板格式，生成数据模型规范。不确定的地方列在待确认问题清单中。"`

---

## 功能名称：`[填写]`

## 功能简述

`[一两句话说清楚这个功能干什么]`

---

## 一、数据对象

> 列出这个功能涉及的每一种"东西"。每个对象一张表。

### 对象：`[中文名称，例如：订单]`

| 信息名称 | 说明 | 是否必须有 | 可选值 / 格式要求 |
|---------|------|----------|-----------------|
| `[例：订单编号]` | `[每个订单的唯一编号]` | `[必须，系统自动生成]` | `[格式：ORD-年月日-序号]` |
| `[例：客户姓名]` | `[下单客户的名字]` | `[必须]` | `[最多20字]` |
| `[...]` | `[...]` | `[...]` | `[...]` |

### 对象：`[第二个对象，例如：订单商品]`

| 信息名称 | 说明 | 是否必须有 | 可选值 / 格式要求 |
|---------|------|----------|-----------------|
| | | | |

### 对象之间的关系

```
[用大白话说，例如：]
- 一个订单可以包含多个商品
- 一个订单属于一个客户
- [...]
```

---

## 二、状态定义（如果有的话）

> 如果你的数据对象有"状态"概念，在这里写清楚。没有就删掉这一节。

**哪个对象有状态**：`[例如：订单]`

**所有可能的状态**：`[例如：待确认、待发货、已发货、已完成、已取消]`

**新建时默认什么状态**：`[例如：待确认]`

**状态之间怎么变化**：

| 从 | 到 | 什么条件下变 | 谁能操作 | 变了之后还要做什么 |
|----|---|------------|---------|---------------|
| `[待确认]` | `[待发货]` | `[点击确认订单]` | `[运营、老板]` | `[无]` |
| `[待确认]` | `[已取消]` | `[点击取消]` | `[运营、老板]` | `[要填取消原因]` |
| `[...]` | `[...]` | `[...]` | `[...]` | `[...]` |

**不允许的变化**：
- `[例如：已完成不能变成已取消]`
- `[...]`

---

## 三、计算规则（如果有的话）

> 涉及金额、数量、比率等计算时写在这里。没有就删掉。

```
[用公式或大白话说清楚，例如：]

商品小计 = 单价 × 数量
订单总金额 = 所有商品小计之和
金额精度要准确，不能出现 0.1+0.2≠0.3 的问题
金额显示时前面加"¥"，保留两位小数
```

---

## 四、校验要求

> 列出你对数据输入的要求。写中文就行。

| 信息名称 | 要求 | 不符合时提示什么 |
|---------|------|----------------|
| `[客户姓名]` | `[不能为空，最多20字]` | `["请输入客户姓名"]` |
| `[手机号]` | `[11位手机号]` | `["请输入正确的手机号"]` |
| `[...]` | `[...]` | `[...]` |

---

## 五、和其他功能的关系

```
[这个功能和哪些已有/待开发的功能有关联：]

- [例：订单中的商品来自"商品管理"功能（已开发/待开发）]
- [例：订单处理会影响"库存管理"的库存数量]
- [例：暂时不和其他功能有关联]
```
