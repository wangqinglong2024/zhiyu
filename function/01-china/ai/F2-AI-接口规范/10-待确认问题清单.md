# F2-AI · 待确认问题清单

> AI 已按"先实现后确认"在前序文件落地默认方案；以下问题请 PM 确认或修正。

| 编号 | 问题 | AI 默认方案 | 需要 PM 确认 |
|------|------|-----------|-------------|
| Q1 | C 端接口的 URL 是否暴露 `code`（业务编码）而非 `id`（uuid）？示例：`/api/v1/china/articles/A7K2P9X3M4QR` vs `/api/v1/china/articles/<uuid>` | 默认对 C 端用 `code`（更短、SEO 友好、与 PM "文章 ID + 句子 ID 是缓存键"语义一致）；管理端用 `id`。 | 是否同意？ |
答案：同意

| Q2 | 应用端"全文朗读"是否需要后端预先一次性把所有句子转成 `ready`？ | 不预生成；按 PM 答 Q10「用户点击触发」，全文朗读由前端逐句调 C4，遇 pending 触发、遇 ready 直接播。 | 是否符合预期？ |
答案：同意

| Q3 | TTS 限流「6 次/min/IP」是否合理？防恶意但又要兼容多用户共享出口 IP（NAT）。 | 默认 6 次/min/IP；可按需调高到 30。 | 阈值是否需要调整？ |
答案：20次/min吧，主要是已经有用户点击过的句子，不要重复调接口，这是重点。

| Q4 | 文章删除（A8）是否要求"二次确认"在接口层？ | 不在接口层，由前端做确认对话框；接口直接执行软删。 | 是否同意？ |
答案：前端确认，同意

| Q5 | 句子翻译"必填"在 `draft` 阶段是否也强制？还是只有发布时才校验？ | A11/A12 当前**强制**所有 5 语言 + 拼音必填（与 F1 NOT NULL 约束一致）。如需"草稿允许残缺"，要给所有 5 列加可空 + 在 A6 发布时校验。 | 当前默认是否符合 PM 用法？PM 是"准备好整套数据再录入"的工作流（答 Q3），所以默认应该 OK。 |
答案：保存时就校验，不允许残缺；

| Q6 | A14 重排接口是否要求传入「所有」句子 id，还是允许"部分调换"？ | 默认要求**全量**列表（更稳，不易出错）；前端按当前列表整体提交。 | 是否需要支持局部调换 API？ |
答案：全部传入不要出错。

| Q7 | 多端编辑提示策略——是否需要后端返回 `version`/`etag` 协助前端检测冲突？ | 默认走 `updated_at + updated_by` 弱检测；不引入 etag。 | 是否同意？ |
答案：同意

| Q8 | 应用端文章详情（C3）是否需要"上一篇/下一篇"导航字段？ | 当前不返回；如需要可在响应里加 `prev_code` / `next_code`（按 `published_at` 同类目排序）。 | 是否需要？ |
答案：需要

| Q9 | 用户进度（C6/C7）跨域表 `learning_progress` 命名是否预留正确？ | 暂用 `(user_id, source='china', source_code=article_code, last_seq_no, completed)`；待用户进度域 F1 出来后再对齐。 | 命名是否提前定？ |
答案：你决定

| Q10 | 全文朗读是否需要"句间停顿"参数？ | 不在接口层；由前端控制（默认 300ms）。 | 是否同意？ |
答案：可以，前端控制

| Q11 | C 端文章列表（C2）是否对未登录用户也提供？ | 是，公开。 | 是否需要登录墙？ |
答案：前三个类目提供，后九个只能看类目列表，点击时跳转登录页。

| Q12 | 类目卡片接口是否需要 `Cache-Control` 强缓存？ | 默认 `public, max-age=3600`；类目变更走代码发布，1 小时过期可接受。 | 是否同意？ |
答案：可以，类目基本不会变更