# F2-用户：操作与业务逻辑

**提示词**：`"请根据我的技术偏好，
按照F2-AI模板的格式，/opt/projects/zhiyu/template/all_totals/F2-AI-接口规范.md
生成完整的架构与技术规范。不确定的地方列在待确认问题清单中。"`

必须严格按照模板内容和我的偏好输出，输出位置为/opt/projects/zhiyu/function/01-china/ai;
禁止你在创建或更新文件时，单文件内容超过1200行，如果文件过大，必须将原文件拆分为多个，放在一个文件夹下。
有你不明确的内容时，必须向我提问，但是不要终止当前对话。没有完成你要做事的事情之前，禁止你自行终止。

再就是你输出的时候，要把当前F2-AI模板的内容按照你新生成的结果，拆分为多个子文件，便于未来AI可以快速定位查找具体规范。


---

## 功能名称：`[填写，与F1一致]`

---

## 一、这个功能需要哪些操作

> 用大白话列出用户在这个功能里能做哪些事情。不用写接口路径，只说"做什么"。

```
[例如：]

1. 查看列表：能看到所有订单，支持搜索（按订单号、客户名）、筛选（按状态、日期范围）、分页、排序
2. 查看详情：点击某个订单看它的完整信息
3. 新建：手动录入一个新订单
4. 编辑：修改订单的客户信息、备注等（已发货的不能改）
5. 确认订单：把"待确认"变成"待发货"
6. 确认发货：填入快递单号，变成"已发货"
7. 确认收货：变成"已完成"
8. 取消订单：需要填取消原因
9. 重新打开：把已取消的订单恢复（仅老板能操作）
10. 导出：把当前列表导出为Excel
11. 添加备注：随时给订单添加备注
```

---

## 二、每个操作谁能用

> 简单标注每个操作的权限。

| 操作 | 谁能用 |
|------|--------|
| `[查看列表]` | `[所有人]` |
| `[新建]` | `[运营、老板]` |
| `[确认订单]` | `[运营、老板]` |
| `[重新打开]` | `[仅老板]` |
| `[导出]` | `[运营、老板]` |
| `[...]` | `[...]` |

---

## 三、操作的特殊规则

> 某些操作有条件限制或特殊逻辑时写在这里。

```
[例如：]

编辑订单：
- 只有"待确认"和"待发货"状态的订单可以编辑
- "已发货"之后不能再编辑

确认发货：
- 必须填入快递单号才能确认
- 快递单号只允许字母和数字

取消订单：
- "已发货"的订单不能直接取消
- 取消时必须填写原因

导出：
- 导出的是当前筛选条件下的数据，不是全部数据
- 导出格式为Excel
```

---

## 四、边界情况

> 写你能想到的"如果...怎么办"。

| 情况 | 怎么处理 |
|------|---------|
| `[两个人同时处理同一个订单]` | `[谁先提交谁生效，后面的提示"已被xxx处理"]` |
| `[快递单号填错了]` | `[发货后24小时内可以修改]` |
| `[导出的数据量很大]` | `[先不管性能，后续优化]` |
| `[...]` | `[...]` |

---

## 五、补充说明

```
[任何关于操作逻辑的补充，例如：]
- 新建订单功能第一期先不做
- 后续可能增加批量操作（批量发货等）
```
