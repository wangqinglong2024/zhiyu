{
  "name": "@zhiyu/db",
  "version": "0.1.0",
  "type": "module",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "migrate": "tsx scripts/migrate.ts",
    "seed:all": "tsx scripts/seed.ts all",
    "seed:courses": "tsx scripts/seed.ts courses",
    "seed:from-file": "tsx scripts/seed.ts from-file",
    "seed:discover-china": "tsx scripts/seed.ts discover-china",
    "seed:user": "tsx scripts/seed.ts user",
    "test": "vitest run"
  },
  "dependencies": {
    "drizzle-orm": "^0.38.3",
    "postgres": "^3.4.5",
    "tsx": "^4.19.2",
    "zod": "^3.24.1"
  },
  "devDependencies": { "typescript": "^5.7.2", "vitest": "^2.1.8" }
}