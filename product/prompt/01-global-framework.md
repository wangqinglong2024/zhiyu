# 提示词 01：应用端 — 全局框架与导航系统 PRD

---

## 你的角色

你是「知语 Zhiyu」的高级产品经理，正在为开发团队编写**开发级产品需求文档（PRD）**。你写的文档必须精确到每个按钮的标签文字、每个操作的触发条件和结果、每个页面的 7 种状态表现，让前端和后端开发人员拿到后**零疑问直接开发**。

---

## 本次任务

生成**应用端全局框架与导航系统**的详细 PRD，覆盖范围：

1. **底部 Tab 导航栏**（4 个 Tab 的图标、文字、选中态、切换逻辑、角标规则）
2. **登录与注册系统**（登录弹窗、Google/Apple/邮箱登录、注册流程、忘记密码、推荐码输入、登录墙触发场景与表现）
3. **多语言切换系统**（UI 语言切换、学习语言模式、解释语言开关、配置联动规则）
4. **深色/浅色模式切换**（切换方式、跟随系统、持久化）
5. **推送通知系统**（通知类型、触发条件、通知内容模板、点击后跳转目标）
6. **全局加载/错误/空状态规范**（骨架屏规范、错误提示文案、空状态插图+引导、离线状态处理、网络恢复处理）
7. **全局通用组件定义**（Toast 规范、模态弹窗规范、底部弹出面板规范、下拉刷新/上拉加载规范）
8. **PWA 相关行为**（安装提示、离线缓存策略、更新提示）

---

## 必读参考文件（请在生成前完整阅读以下文件）

- `/opt/projects/ideas_ai/product/00-product-overview.md` — 产品总纲（用户分层、访问规则、多语言体系、核心业务规则）
- `/opt/projects/ideas_ai/product/apps/00-apps-overview.md` — 应用端概述（Tab 结构、登录注册流程、推送通知、全局设计规范）
- `/opt/projects/ideas_ai/grules/product-design.md` — PRD 规范（8 维度质量铁律、状态矩阵、用户故事格式、验收标准要求）
- `/opt/projects/ideas_ai/grules/ui-design.md` — UI 设计规范（Cosmic Refraction 设计系统、色彩体系、导航规范、Toast 规范、表单规范、动效规范）
- `/opt/projects/ideas_ai/plan/01-executive-summary.md` — 执行摘要（多语言体系设计 §三）

---

## 输出文件路径

`/opt/projects/ideas_ai/product/apps/01-global-framework.md`

---

## 输出结构（请严格按此结构生成，不可遗漏任何章节）

```
# 应用端全局框架与导航系统 — 详细 PRD

## 一、文档信息
- 版本、日期、关联文档

## 二、功能清单总览
- P0/P1/P2 优先级功能表（含用户故事和验收标准）

## 三、底部 Tab 导航栏
- 3.1 Tab 结构定义（图标、文字、选中/未选中态）
- 3.2 Tab 切换逻辑（未登录用户切换到需登录 Tab 时的行为）
- 3.3 角标规则（如有新内容/未读通知时的角标表现）
- 3.4 Tab Bar 视觉规范（高度、安全区、滚动行为）
- 3.5 状态矩阵（7 种状态的 Tab Bar 表现）

## 四、登录与注册系统
- 4.1 登录墙触发场景清单（逐一列出所有触发场景和触发条件）
- 4.2 登录弹窗（非全屏）详细设计
  - 弹窗布局描述
  - Google 一键登录按钮（标签文字、图标、点击后流程、成功/失败处理）
  - Apple 一键登录按钮（同上）
  - 邮箱登录区（邮箱输入框、密码输入框、校验规则、登录按钮、加载态）
  - "忘记密码"链接（点击后流程）
  - 切换到注册的入口
- 4.3 注册流程
  - 注册表单字段（邮箱、密码、昵称、推荐码）
  - 每个字段的校验规则（格式、长度、实时校验时机）
  - 隐私协议+服务条款勾选（必勾）
  - 推荐码输入规则（可选字段，校验逻辑）
  - 注册按钮行为（加载态、成功跳转、失败提示）
- 4.4 忘记密码流程
  - 邮箱输入 → 验证码发送 → 验证码校验 → 新密码设置 → 重置成功
  - 每一步的页面描述、字段、按钮、异常处理
- 4.5 登录态管理（Token 过期、自动登录、异地登录）
- 4.6 登出流程（二次确认、清理行为）
- 4.7 状态矩阵

## 五、多语言切换系统
- 5.1 UI 语言切换（支持语言列表、切换入口位置、切换后页面刷新行为）
- 5.2 学习语言模式（拼音+中文 / 纯中文、切换入口、切换后内容渲染变化）
- 5.3 解释语言开关（开启/关闭、跟随 UI 语言、特殊规则：中文 UI 时禁用）
- 5.4 语言配置联动关系图
- 5.5 首次打开 App 的语言初始化逻辑
- 5.6 状态矩阵

## 六、深色/浅色模式
- 6.1 切换方式（浅色/深色/跟随系统）
- 6.2 切换入口位置
- 6.3 切换后的页面表现（即时生效、动画过渡）
- 6.4 持久化（下次打开保持选择）

## 七、推送通知系统
- 7.1 通知权限请求时机和方式
- 7.2 通知类型清单（逐一定义：类型名称、触发条件、通知标题模板、通知内容模板、点击后跳转目标页面）
- 7.3 通知开关（用户可在设置中关闭）
- 7.4 应用内通知表现（若 App 在前台时收到通知的表现）

## 八、全局状态规范
- 8.1 加载状态（骨架屏规范：形状、动画、持续条件）
- 8.2 首次加载（全屏骨架屏 + 品牌 Logo 呼吸动画）
- 8.3 空状态（插图 + 引导文案 + CTA 按钮的标准结构）
- 8.4 错误状态（错误文案规范、重试按钮）
- 8.5 部分加载（列表底部加载指示器、下拉刷新）
- 8.6 离线状态（顶部 Banner 提示、缓存数据可浏览）
- 8.7 网络恢复（自动刷新当前页数据、Banner 消失）

## 九、全局通用组件
- 9.1 Toast 组件（位置、动画、持续时间、类型、堆叠规则、关闭规则）
- 9.2 模态弹窗（尺寸、背景遮罩、关闭方式、按钮布局、危险操作二次确认规范）
- 9.3 底部弹出面板 Bottom Sheet（拉出高度、拖拽关闭、内容规范）
- 9.4 下拉刷新（触发距离、动画、刷新完成反馈）
- 9.5 上拉加载更多（触发距离、加载指示器、无更多数据提示）

## 十、PWA 行为
- 10.1 安装提示（触发条件、提示样式、忽略后的再次提示策略）
- 10.2 离线缓存（哪些内容离线可用、哪些必须在线）
- 10.3 版本更新提示（检测到新版本时的提示方式和更新流程）

## 十一、非功能需求
- 首屏加载时间、Tab 切换响应时间、API 超时策略

## 十二、验收标准汇总
- 按功能模块列出可测试的验收条件
```

---

## 质量要求

1. **禁止定义任何技术实现细节**：不写字段英文名、不写 API 路径、不写 CSS 类名、不写数据库表名
2. **每个可交互元素必须说清**：按钮的标签文字是什么、什么时候可点击、什么时候置灰、点击后发生什么、失败了怎么提示
3. **每个输入框必须说清**：占位提示文字、校验规则（格式+长度+时机）、错误提示文案
4. **每个页面必须覆盖 7 种状态**：空状态、加载中、首次加载、成功、错误、部分加载、离线
5. **用户故事格式**：「作为 [角色]，我想要 [操作]，以便 [目的]」
6. **严格遵循 ui-design.md 的设计规范**：色彩用 Rose/Sky/Amber 描述而非色值、间距用 8 的倍数、圆角引用设计系统 Token 名

---

## 特别注意

- 未登录用户可以正常浏览发现中国前 3 类目，切换到其他 Tab（课程/游戏/个人中心）时触发登录墙
- 登录弹窗是非全屏弹窗，保持用户上下文
- 多语言切换的特殊规则：UI 语言选中文时，解释语言开关必须自动关闭且不可开启（中文解释中文无意义）
- 推送通知不要在首次打开 App 时就请求权限，应在合适时机（如完成首课后）
- 本文档是所有后续 PRD 的基础，定义的全局规范会被所有模块引用
