# 推送管理

> 关联文档：[管理后台概述 §七](../00-admin-overview.md) | [运营策略](../../../plan/08-operations-marketing.md)
> 路由路径：`/admin/push`
> 角色权限：超级管理员、内容运营、用户运营、游戏运营
> 情绪目标：**精准、高效、谨慎** — 确保推送内容准确触达目标用户，避免骚扰无关用户

---

## 一、推送列表页

### 1.1 页面布局

| 属性 | 说明 |
|------|------|
| **页面标题** | "推送管理"，H1 级别（32px，字重 700） |
| **标题右侧** | "新建推送"按钮，Rose 色实心按钮（`rounded-full`，高度 40px），Lucide `Plus` 图标 16px + 文字"新建推送" |
| **筛选栏** | 标题下方，间距 16px |
| **列表区域** | 筛选栏下方，间距 16px |

### 1.2 筛选栏

| 属性 | 说明 |
|------|------|
| **布局** | 一行排列，各筛选项间距 12px |
| **样式** | 毛玻璃下拉（`.glass-input`），圆角 `rounded-md`（12px） |
| **重置** | 右末尾"重置筛选"文字按钮，Sky 色 |

#### 筛选项 1：推送状态

| 属性 | 说明 |
|------|------|
| **组件** | 下拉单选 |
| **标签** | "状态" |
| **选项** | 全部（默认）/ 草稿 / 待发送 / 已发送 / 发送失败 |

#### 筛选项 2：推送类型

| 属性 | 说明 |
|------|------|
| **组件** | 下拉单选 |
| **标签** | "推送类型" |
| **选项** | 全部（默认）/ 学习提醒 / 活动通知 / 系统通知 |

#### 筛选项 3：发送日期范围

| 属性 | 说明 |
|------|------|
| **组件** | 日期范围选择器 |
| **标签** | "发送日期" |
| **默认值** | 空（不限制） |

### 1.3 推送列表表格

#### 表格布局

| 属性 | 说明 |
|------|------|
| **样式** | 毛玻璃面板（`.glass-card`），圆角 `rounded-2xl`（24px） |
| **表头** | 背景色 `rgba(255,255,255,0.03)`，Caption 级别 |
| **行高** | 56px |
| **分页** | 每页 20 条 |

#### 列定义

| # | 列名 | 宽度 | 内容说明 | 排序 |
|---|------|------|---------|------|
| 1 | 推送标题 | 240px | 中文标题，Body 级别，白色；超长截断 `text-overflow: ellipsis`，Hover Tooltip 显示完整标题 | 否 |
| 2 | 推送类型 | 120px | 标签样式：学习提醒 = Sky 色标签；活动通知 = Amber 色标签；系统通知 = 灰色标签 | 否 |
| 3 | 目标人群 | 160px | 简要描述（如"全部用户"、"7天未登录"、"付费用户"）；条件复杂时截断 + Tooltip 展示完整条件 | 否 |
| 4 | 推送时间 | 140px | 已发送：实际发送时间 YYYY-MM-DD HH:mm；待发送：计划时间 + Amber 色"定时"标签；草稿：显示"—" | ✅ 默认降序 |
| 5 | 状态 | 100px | "草稿" = 灰色圆点 + 文字；"待发送" = Amber 色圆点 + 文字；"已发送" = 绿色圆点 + 文字；"发送失败" = 红色圆点 + 文字 | 否 |
| 6 | 送达率 | 80px | 百分比，等宽字体；仅"已发送"状态显示，其余显示"—"；≥90% 绿色，<70% 红色 | ✅ |
| 7 | 操作 | 200px | 操作按钮组，详见 §1.4 | 否 |

### 1.4 操作列

| 按钮 | 样式 | 条件 | 点击行为 |
|------|------|------|---------|
| **编辑** | 文字按钮，Sky 色 | "草稿"或"待发送"状态 | 跳转至 `/admin/push/{pushId}/edit` |
| **查看** | 文字按钮，Sky 色 | "已发送"或"发送失败"状态 | 跳转至 `/admin/push/{pushId}/detail`（只读） |
| **发送** | 文字按钮，Rose 色 | 仅"草稿"状态 | 打开发送确认弹窗（详见 §2.8） |
| **取消发送** | 文字按钮，Amber 色 | 仅"待发送"状态 | 确认弹窗"确认取消定时发送？" → 确认后状态回退为"草稿" |
| **重试** | 文字按钮，Amber 色 | 仅"发送失败"状态 | 重新发送确认弹窗 → 重试发送 |
| **删除** | 文字按钮，错误色 | 仅"草稿"状态 | 删除确认弹窗"确认删除此推送？" → 软删除 |

按钮间距 16px，水平排列。

---

## 二、新建/编辑推送

> 路由路径：新建 `/admin/push/create` | 编辑 `/admin/push/{pushId}/edit`
> 情绪目标：**精准、从容** — 三语内容并排编辑，精确定位目标人群

### 2.1 页面布局

| 属性 | 说明 |
|------|------|
| **页面标题** | 新建时："新建推送"；编辑时："编辑推送" |
| **返回** | 标题左侧 Lucide `ArrowLeft`，返回推送列表 |
| **表单区域** | 毛玻璃面板（`.glass-card`），最大宽度 900px，水平居中 |
| **底部按钮栏** | 固定在页面底部，毛玻璃背景，高度 72px |

### 2.2 推送标题区

区块标题："推送标题"，H3 级别，下方间距 16px。

#### 字段 1：推送标题（中文）

| 属性 | 说明 |
|------|------|
| **组件** | 文本输入框（`.glass-input`，`rounded-full`） |
| **标签** | "标题（中文）"，标红星 |
| **Placeholder** | "请输入中文推送标题" |
| **校验** | 必填，2-50 个字符 |

#### 字段 2：推送标题（英文）

| 属性 | 说明 |
|------|------|
| **组件** | 文本输入框 |
| **标签** | "标题（英文）"，标红星 |
| **Placeholder** | "Enter English push title" |
| **校验** | 必填，2-100 个字符 |

#### 字段 3：推送标题（越南语）

| 属性 | 说明 |
|------|------|
| **组件** | 文本输入框 |
| **标签** | "标题（越南语）"，标红星 |
| **Placeholder** | "Nhập tiêu đề tiếng Việt" |
| **校验** | 必填，2-100 个字符 |

### 2.3 推送内容区

区块标题："推送内容"，H3 级别，下方间距 16px。

#### 字段 4：推送内容（中文）

| 属性 | 说明 |
|------|------|
| **组件** | 多行文本框（`.glass-input`，`rounded-md`），高度 100px |
| **标签** | "内容（中文）"，标红星 |
| **Placeholder** | "请输入中文推送内容" |
| **校验** | 必填，5-200 个字符；右下角显示字数统计 |

#### 字段 5：推送内容（英文）

| 属性 | 说明 |
|------|------|
| **组件** | 多行文本框，高度 100px |
| **标签** | "内容（英文）"，标红星 |
| **校验** | 必填，5-500 个字符 |

#### 字段 6：推送内容（越南语）

| 属性 | 说明 |
|------|------|
| **组件** | 多行文本框，高度 100px |
| **标签** | "内容（越南语）"，标红星 |
| **校验** | 必填，5-500 个字符 |

### 2.4 推送类型选择

区块标题："推送类型"，H3 级别，下方间距 16px。

#### 字段 7：推送类型

| 属性 | 说明 |
|------|------|
| **组件** | 单选组（Radio Group），3 个选项卡片式排列 |
| **标签** | "推送类型"，标红星 |

**选项卡片**：

| 选项 | 图标 | 说明 |
|------|------|------|
| **学习提醒** | Lucide `BookOpen` | "提醒用户回来学习，如连续签到、课程进度提醒" |
| **活动通知** | Lucide `PartyPopper` | "赛季开始/结束、新皮肤上架、节日活动、限时折扣" |
| **系统通知** | Lucide `Settings` | "系统维护公告、版本更新、隐私政策变更" |

每个选项卡片为毛玻璃面板（宽度均分），圆角 `rounded-lg`，选中状态：Rose 色边框 + 背景加亮 `rgba(225,29,72,0.06)`。

### 2.5 目标人群选择

区块标题："目标人群"，H3 级别，下方间距 16px。
区块说明文字："选择推送的目标用户群体，支持多条件组合筛选。"灰色 Body S 级别。

#### 字段 8：目标范围

| 属性 | 说明 |
|------|------|
| **组件** | 单选组（Radio） |
| **选项** | "全部用户"（默认）/ "自定义人群" |
| **联动** | 选择"自定义人群"后，展开以下筛选条件 |

#### 自定义人群筛选条件

##### 条件 1：用户类型

| 属性 | 说明 |
|------|------|
| **组件** | 多选复选框组 |
| **标签** | "用户类型" |
| **选项** | 游客 / 免费用户 / 付费用户 |
| **默认** | 全部勾选 |

##### 条件 2：最近活跃时间

| 属性 | 说明 |
|------|------|
| **组件** | 下拉单选 |
| **标签** | "最近活跃" |
| **选项** | 不限（默认）/ 3 天内活跃 / 7 天未登录 / 14 天未登录 / 30 天未登录 / 自定义天数 |
| **自定义** | 选择"自定义天数"后，展开数字输入框"X 天未登录"（正整数，1-365） |

##### 条件 3：国家/地区

| 属性 | 说明 |
|------|------|
| **组件** | 多选下拉（带搜索） |
| **标签** | "国家/地区" |
| **选项** | 全部（默认）/ 越南 / 印度尼西亚 / 中国 / 美国 / 其他（按用户数量排序） |

##### 条件 4：段位范围

| 属性 | 说明 |
|------|------|
| **组件** | 双端下拉（最低段位 ~ 最高段位） |
| **标签** | "段位范围" |
| **选项** | 无段位 / 青铜 / 白银 / 黄金 / 铂金 / 钻石 / 星耀 / 王者 |
| **默认** | 不限 |
| **校验** | 最低段位 ≤ 最高段位 |

##### 条件 5：课程进度

| 属性 | 说明 |
|------|------|
| **组件** | 下拉单选 |
| **标签** | "课程进度" |
| **选项** | 不限（默认）/ 未开始学习 / L1-L3 学习中 / L1-L3 已完成 / L4+ 付费用户 |

#### 预估人数

| 属性 | 说明 |
|------|------|
| **位置** | 目标人群区块底部，灰色边框分隔线上方 |
| **内容** | "预估目标人数：约 XX,XXX 人"，H3 级别，Amber 色数字 |
| **更新时机** | 每次修改筛选条件后自动请求后端接口，500ms 防抖，返回预估人数 |
| **加载态** | 数字位置显示 Skeleton 短条 |

### 2.6 推送时间

区块标题："推送时间"，H3 级别，下方间距 16px。

#### 字段 9：发送时间

| 属性 | 说明 |
|------|------|
| **组件** | 单选组（Radio） |
| **选项** | "立即发送" / "定时发送" |
| **联动** | 选择"定时发送"后，展开日期+时间选择器 |

#### 字段 10：定时发送日期和时间

| 属性 | 说明 |
|------|------|
| **组件** | 日期选择器 + 时间选择器（精确到分钟），并排 |
| **标签** | "计划发送时间"，标红星（定时发送时） |
| **校验** | 必须晚于当前时间至少 5 分钟 |
| **辅助文案** | "时区：UTC+8（北京时间）" |

### 2.7 推送预览

区块标题："推送预览"，H3 级别，下方间距 16px。

| 属性 | 说明 |
|------|------|
| **位置** | 表单区域底部，操作按钮上方 |
| **内容** | 模拟手机推送通知卡片效果，3 个语言版本横排展示 |
| **卡片样式** | 每个预览卡片宽度 280px，圆角 `rounded-lg`，灰色背景模拟手机通知栏 |
| **卡片内容** | 顶部：App 图标 + "知语 Zhiyu"；标题：对应语言的推送标题（加粗）；内容：对应语言的推送内容（最多 3 行，超出截断 + "..."） |
| **标签** | 每个卡片下方标注语言名称："中文"/"English"/"Tiếng Việt" |

### 2.8 底部操作按钮

| 按钮 | 样式 | 点击行为 |
|------|------|---------|
| **取消** | 灰色描边按钮 | 返回推送列表（未保存提醒） |
| **保存草稿** | Sky 色描边按钮 | 校验必填字段 → 保存为草稿 → Toast"已保存草稿"→ 返回列表 |
| **确认发送** | Rose 色实心按钮 | 校验全部字段 → 打开发送确认弹窗 |

#### 发送确认弹窗

| 属性 | 说明 |
|------|------|
| **弹窗标题** | "确认发送推送" |
| **弹窗内容** | 推送标题（中文）+ 推送类型 + 目标人群描述 + 发送时间 |
| **核心确认文案** | **"确认发送给约 {预估人数} 人？"**，数字使用 H2 级别 Rose 色加粗 |
| **按钮** | "取消"（灰色描边）+ "确认发送"（Rose 色实心） |
| **确认后** | 立即发送：状态 → "已发送"，后台异步执行推送；定时发送：状态 → "待发送"，到达指定时间自动发送 |
| **Toast** | 立即发送："推送已发送"（绿色）；定时发送："推送已设定，将于 {时间} 发送"（绿色） |

---

## 三、推送数据统计

> 路由路径：`/admin/push/{pushId}/detail`
> 优先级：P1

### 3.1 页面布局

| 属性 | 说明 |
|------|------|
| **页面标题** | "推送详情"，H1 级别 |
| **返回** | 标题左侧 Lucide `ArrowLeft`，返回推送列表 |

### 3.2 推送信息区

| 字段 | 说明 |
|------|------|
| 推送标题 | 三语并排展示 |
| 推送内容 | 三语并排展示 |
| 推送类型 | 标签样式 |
| 目标人群 | 完整条件描述 |
| 发送时间 | YYYY-MM-DD HH:mm |
| 状态 | 已发送/发送失败 |

### 3.3 效果数据卡片（4 个横排）

| 卡片 | 指标 | 说明 |
|------|------|------|
| 卡片 1 | 目标人数 | 推送发出时的目标用户总数 |
| 卡片 2 | 送达率 | 成功送达数 / 目标人数 × 100%；≥90% 绿色，<70% 红色 |
| 卡片 3 | 打开率 | 打开推送的用户数 / 成功送达数 × 100% |
| 卡片 4 | 点击率 | 点击推送跳转的用户数 / 打开用户数 × 100% |

卡片样式：毛玻璃面板，圆角 `rounded-2xl`，内边距 24px。

### 3.4 推送效果汇总（P1）

> 路由路径：`/admin/push/analytics`

| 属性 | 说明 |
|------|------|
| **入口** | 推送列表页标题右侧"查看统计"文字按钮（Sky 色） |
| **内容** | 按推送类型分组的平均送达率、打开率、点击率统计表 |
| **图表** | 折线图展示近 30 天推送数据趋势（送达率/打开率/点击率） |

---

## 四、状态矩阵

### 4.1 推送列表页

| 状态 | 表现 |
|------|------|
| **空状态 (Empty)** | 表格区域居中显示 Lucide `Bell` 48px 灰色图标 + "暂无推送记录"文案 + "新建推送"Rose 色 CTA 按钮 |
| **加载中 (Loading)** | 表格区域 Skeleton 骨架屏（10 行） |
| **首次加载 (First Load)** | 全表格 Skeleton + 品牌 Logo 呼吸动画 |
| **成功 (Success)** | 正常展示列表；操作成功后顶部 Toast |
| **错误 (Error)** | "加载失败，请重试"+ Lucide `AlertCircle` 图标 + "重新加载"按钮 |
| **部分加载 (Partial)** | 翻页时底部加载指示器 |
| **离线 (Offline)** | 页面顶部 Amber 色 Banner："网络连接中断" |

### 4.2 新建/编辑推送页

| 状态 | 表现 |
|------|------|
| **空状态 (Empty)** | 新建时表单为空 |
| **加载中 (Loading)** | 编辑时表单 Skeleton 骨架屏 |
| **首次加载 (First Load)** | 同上 |
| **成功 (Success)** | 发送/保存成功 → Toast → 返回列表 |
| **错误 (Error)** | 发送失败 → Toast"发送失败，请重试"（红色）→ 表单内容保留 |
| **部分加载 (Partial)** | 预估人数计算中显示 Skeleton |
| **离线 (Offline)** | 顶部 Banner + 发送按钮 Disabled |
