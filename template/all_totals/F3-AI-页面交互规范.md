# F3-AI：页面交互规范

> **谁来写**：AI根据《F3-用户：页面与交互描述》生成
> **谁来审**：产品经理
> **何时引用**：编码阶段逐个开发页面时使用。每次只取其中一个页面的定义喂给编码AI。
>
> **AI指令**：
> 1. 样式和组件严格遵循G2-AI中的设计系统
> 2. 数据字段严格遵循F1-AI中的表定义
> 3. 接口调用严格遵循F2-AI中的接口定义
> 4. 每一条交互行为都必须有对应的技术实现方案
> 5. 生成完成后必须做一次场景验证（见文末）

---

## 功能名称：`[从F3-用户复制]`

---

## 一、页面详细规格

### 页面：`[页面名称]`

**路由**：`[AI定义]`

**面包屑**：`[AI定义]`

**权限**：`[引用G3-AI的角色定义]`

#### 布局

```
[AI用文本框画出精确的布局结构，标注每个区域]

┌───────────────────────────────────────────────────┐
│  标题"xxx"                         [按钮A] [按钮B]  │
├───────────────────────────────────────────────────┤
│ [搜索框___] [筛选1▾] [筛选2▾] [重置]                │
├───────────────────────────────────────────────────┤
│  列1  │  列2  │  列3  │  列4  │  列5  │  操作    │
│       │       │       │       │       │          │
├───────────────────────────────────────────────────┤
│                    分页器                          │
└───────────────────────────────────────────────────┘
```

#### 元素规格

| 元素 | 组件类型 | 规格 | 可见角色 |
|------|---------|------|---------|
| | | | |

#### 表格列定义（如有表格）

| 列标题 | 数据字段(引用F1-AI) | 宽度 | 排序 | 渲染方式 |
|--------|-------------------|------|------|---------|
| | | | | |

#### 筛选区定义（如有筛选）

| 筛选项 | 组件类型 | 数据源 | 默认值 |
|--------|---------|--------|--------|
| | | | |

---

### 弹窗/面板：`[名称]`

**触发方式**：`[什么操作打开它]`

**组件类型**：`[Modal / Drawer / 页面]`

**尺寸**：`[宽度、从哪侧滑出等]`

#### 布局

```
[AI画出弹窗/面板内部结构]
```

#### 表单字段定义（如有表单）

| 字段标签 | 组件类型 | 数据字段(引用F1-AI) | 必填 | 校验规则(引用F1-AI) | 说明 |
|---------|---------|-------------------|------|-------------------|------|
| | | | | | |

#### 条件显示规则

| 条件 | 显示什么 | 隐藏什么 |
|------|---------|---------|
| `[当状态=xxx时]` | `[显示这些元素]` | `[隐藏这些元素]` |

---

`[对每个页面/弹窗重复完整格式]`

---

## 二、交互行为技术方案

> 将F3-用户中的每一条交互行为翻译为精确的技术实现。

| 需求编号 | 用户操作(来自F3-用户) | 技术实现方案 |
|---------|---------------------|------------|
| 01 | | 组件挂载时调用 `[引用F2-AI中的接口]`，参数：`[具体参数]`。数据返回前显示 `[骨架屏/loading]`（遵循G2-AI规范） |
| 02 | | 对输入值做 `[X]ms` 防抖，调用 `[接口]` 带 `[参数]`，page重置为1 |
| 03 | | 打开 `[Modal/Drawer]` 组件，调用 `[接口]` 获取数据 |
| 04 | | 前端校验 `[字段]`（规则引用F1-AI），校验失败在字段下方显示错误提示（样式遵循G2-AI） |
| 05 | | 前端校验通过 → 按钮变loading → 调用 `[接口]` → 成功：`[关闭弹窗 + 更新列表 + Toast提示]` → 失败：`[不关闭 + 显示错误]` |
| | | |

> **每一条都必须填写，不允许跳过任何一条。**
> **如果F3-用户遗漏了某些必要的交互（如加载状态、空状态），AI在此补充并标注"AI补充"。**

---

## 三、AI补充的交互行为

> F3-用户中未提及，但技术上必须处理的交互。

| 编号 | 场景 | AI的实现方案 | PM确认 |
|------|------|------------|--------|
| A01 | `[例：列表加载失败时]` | `[显示错误提示+重试按钮]` | |
| A02 | `[例：弹窗中未保存就关闭]` | `[弹出确认提示]` | |
| | | | |

---

## 四、场景验证

> AI生成完本文档后，必须自行走一遍完整的用户操作流程，验证逻辑无断裂。

### 场景：`[描述一个核心操作流程]`

```
1. 用户 [角色] 登录系统
   → 认证方式：[引用G3-AI]
   → ✅/❌

2. 进入 [页面名称]
   → 路由：[路径]
   → 调用接口：[引用F2-AI]
   → 页面渲染：[引用本文档的布局定义]
   → ✅/❌

3. 执行 [操作]
   → 触发交互行为编号：[编号]
   → 调用接口：[引用F2-AI]
   → 数据变更：[引用F1-AI的字段]
   → 页面更新：[描述更新内容]
   → ✅/❌

4. [继续下一步操作...]
   → ✅/❌

验证结果：[逻辑连贯/发现断裂]
[如果发现问题，在此说明并修复]
```

---

## 五、待确认问题清单

| 编号 | 问题 | AI的默认方案 | 需要PM确认 |
|------|------|------------|-----------|
| Q1 | | | |

---

## 六、开发对照清单

> 编码AI完成每个页面后必须填写此表。

### 页面：`[页面名称]`

| 交互编号 | 需求描述 | 实现状态 | 偏差说明 |
|---------|---------|---------|---------|
| 01 | | ✅/⚠️/❌ | |
| 02 | | | |

**AI自行补充的实现**：
- `[列出需求中没提到但自己补充的实现细节]`

---

> **PM审核要点**：
> ✅ 每个页面的布局是否和你描述/截图的一致
> ✅ 表格列、表单字段是否完整
> ✅ 条件显示规则是否正确
> ✅ 每一条交互行为是否都有技术方案
> ✅ AI补充的交互是否合理
> ✅ 场景验证是否通畅
