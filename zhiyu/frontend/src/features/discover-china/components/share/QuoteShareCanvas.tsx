import type { FC } from 'react'

/**
 * Wrapper component for quote share Canvas generation.
 * The actual Canvas logic lives in canvas-share.ts utility.
 * This is a placeholder — generation is triggered imperatively.
 */
export const QuoteShareCanvas: FC = () => null

QuoteShareCanvas.displayName = 'QuoteShareCanvas'
